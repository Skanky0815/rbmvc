<a href="<?php echo $this->url(
        array('action' => 'edit')
    ); ?>">
    &raquo; Eintragen    
</a>
<div class="entries">
<?php foreach ($this->entries as $entry): ?>
    <div class="entry">
        <h3>
            <?php echo $entry->getTitle(); ?>
        </h3>
        <p>
            <?php echo $entry->getText(); ?>
        </p>
        <div class="entry-footer">
            <div class="btn-group">
                <a class="btn-danger btn delete" href="<?php echo $this->url(
                        array('action' => 'delete'
                            , 'id' => $entry->getId()
                        ), true
                ); ?>">
                    <i class="icon-trash icon-white"></i>
                </a>
                <a class="btn" href="<?php echo $this->url(
                        array('action' => 'edit'
                            , 'id' => $entry->getId()
                        ), true
                ); ?>">
                    Edit
                </a>
            </div>
            <div class="creator">        
                <?php echo $entry->getAuthor(); ?>
                <?php echo $this->dateFormater($entry->getDate()); ?>
            </div>
            <div class="clearFix">
            </div>
        </div>
    </div>
<?php endforeach; ?>
</div>
